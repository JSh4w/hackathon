# Railway API Credentials
RAIL_EMAIL=your_email@example.com
RAIL_PWORD=your_password

# OpenAI API Key (for AI analysis feature)
OPENAI_API_KEY=sk-your-openai-api-key

# Environment (set to 'production' in deployment)
NODE_ENV=development

# CORS Origins (comma-separated list of allowed origins)
CORS_ORIGINS=http://localhost:3000,https://your-app.netlify.app
